# 代码评审日志

## 2026-09-12：全项目复核（用户已授权优化修复）

- 已确认：B1 敏感类型未过滤；B2 长文本静默截断；B3 查询失败后孤儿清理误删；B4 常驻期间不清过期项；B5 200 条上限导致旧图片无法访问；B6 图片原件降采样；B7 复制失败先清剪贴板；B8 非法天数触发错误清理；B9 Unicode 大小写搜索；B10 默认语言不一致；B11 文档漂移。
- 待确认：R1 搜索框键盘命令；R2 签名构建 Apple Events 权限；R3 大库/大图响应；R4 缺少充电状态字段时的推断。
- 自检：逐项核对调用链；系统 SQLite 内存验证 201 条图片边界、Unicode LIKE 和查询计划；模拟查询失败分类路径。未读取用户真实历史。
- 用户确认：2026-09-12 明确授权按评审建议优化修复。先记录方案，再实现和验证。


### 2026-09-12 实施后复核

| 项目 | 处理及证据 | 结论 |
|------|------------|------|
| B1 | 读取数据前过滤敏感/临时标记；真实采集路径隔离测试 | 已修复 |
| B2 | 完整文本及换行/NUL保存，绑定长度显式检查；长文本不同尾部测试 | 已修复 |
| B3 | knownImages 查询必须返回 SQLITE_DONE；准备失败返回失败且图片保留测试 | 已修复 |
| B4 | 小时定时器、启动和唤醒清理；过期与固定项测试 | 已修复 |
| B5 | 后台稳定排序与加载更多；202 条分页不丢不重测试 | 已修复 |
| B6 | 原始 PNG/TIFF 数据及独立缩略图；1600×900 PNG 原始字节完全一致 | 已修复，新图片生效 |
| B7 | 图片读取/验证先于 clearContents；失败不走写入分支，后台任务可取消 | 代码复核通过 |
| B8 | 整数和范围校验失败不写偏好、不清理 | 代码复核通过 |
| B9 | unicode_contains 使用 Foundation caseInsensitive；école/俄文/字面通配符测试 | 已修复 |
| B10 | 面板和菜单均使用 Language.systemDefault | 已修复 |
| B11 | API、存储、Language 文件位置和 README 口径同步 | 已修复 |
| R1 | 搜索框 delegate 命令测试通过，标记文本不拦截，异步查询旧结果不执行 | 实际输入法/键盘 UI 待验收 |
| R2 | entitlement 已添加；临时 Hardened Runtime 签名副本验证通过 | TCC 授权交互待验收 |
| R3 | 查询与写入统一后台串行队列；禁用图片不取图片数据；5万行搜索约0.095秒，主线程计时器95次 | 本机合成规模已验证，更大规模待测 |
| R4 | 可空充电字段保留未知状态，不再由接电推断充电 | 硬件返回场景待验收 |

- U1–U6：右键详情、可选快捷键、纯文本语义和复制反馈、操作失败反馈、电量防休眠提示、Finder 手选目录均已实现。
- 自检追加：保留旧绝对路径兼容；容量包括缩略图和新格式；孤立缩略图清理；固定数据超限仍保留；图片异步复制在关闭/重新输入时取消。
- 发布/真实数据：没有安装或运行主应用，没有访问真实历史。构建、签名和运行回归只使用临时路径与独立 pasteboard。

## 2026-06-13：深度评审与体验优化

- **评审结论**：发现已确认问题 4 项，优化建议 2 项
- **范围**：`AppDelegate.swift`、`ClipboardManager.swift`、`ClipboardPanel.swift`、`SystemMonitor.swift`
- **已确认问题**：
  1. 剪贴板面板方向键导航完全失效，方向键触发 `tableViewSelectionDidChange` 导致意外复制并关闭面板。
  2. SQLite 数据库存储了图片的绝对路径，存在跨设备或沙盒化时的图片丢失和无法清理风险。
  3. CPU 占用率计算公式未包含 `nice` 进程，导致特定场景下统计偏低。
  4. Finder 新建文件在 AppleScript 权限被拒时缺乏引导弹窗。
- **优化建议**：
  1. 剪贴板面板底部增加 `Option+Enter` 复制纯文本的隐性高阶功能 UI 提示。
  2. 自定义保留天数时补充占位符和反馈。
- **处理决策**：全部采纳并进入实现修复阶段。

## 2026-06-08：综合项目评审

- **评审结论**：发现已确认问题 6 项，待确认风险 2 项
- **范围**：`AppDelegate.swift`、`ClipboardManager.swift`、`ClipboardPanel.swift`、README 和 `project-log`
- **已确认问题**：
  1. 防休眠 assertion 创建失败后没有回退关闭，UI 和持久化状态可能误导用户。
  2. 剪贴板历史设置为永久保留时，启动清理会跳过孤儿图片清理。
  3. 图片条目删除时先删文件再删数据库，数据库删除失败会留下失效记录。
  4. 防休眠失败回退行为与功能设计文档不一致。
  5. `05-current-status.md` 版本仍为 2.3.8，但工程配置已为 2.3.10。
  6. README 的接电 / 充电电量颜色描述仍写蓝色，与当前实现的粉色不一致。
- **待确认问题**：
  1. SQLite 单连接跨主线程和后台队列访问虽然使用 `FULLMUTEX`，但大历史 / 大图片库下仍可能造成面板卡顿。
  2. 电量 `isCharging` 缺失时用接电状态兜底，可能在部分系统状态下把“已接电”显示成“正在充电”。
- **处理决策**：已确认进入实现；体验优化包括清除历史确认选项、空状态提示、异步缩略图、Finder 文件名输入和更新检查进行中状态。

## 2026-05-12：综合模式标签回归复核

- **评审结论**：问题成立
- **范围**：`AppDelegate.swift` 的综合模式双行标题和紧凑数值路径
- **结果**：`statusItem.length` 的手动计算会影响综合模式双行标题，后续恢复 `NSStatusItem.variableLength`

## 2026-05-11：菜单栏对齐和边界评审

- **评审结论**：部分问题成立
- **范围**：菜单栏对齐、剪贴板面板定位、辅助功能提示
- **结果**：对齐参数收敛，面板定位修正到状态栏按钮自身坐标，辅助功能提示保留系统弹窗

## 2026-05-07：独立复核

- **评审结论**：部分问题成立
- **范围**：开机启动、历史清理、版本号、面板定位
- **结果**：开机启动状态改为以系统真实状态为准，历史清理改为设置变更后立即生效

## 2026-05-04：初始评审

- **评审结论**：发现重复写入风险
- **范围**：剪贴板点击回写、轮询检测、数据库插入
- **结果**：保留为历史问题记录，后续通过同步 changeCount 和重复检测思路处理

## 评审摘要

- 当前已知的主要历史问题都已经在后续版本中被处理或确认可接受
- 新的功能改动仍应在 `10-planning-log.md` 先确认，再进入实现

## 变更记录

| 日期 | 变更内容 | 原因 |
|------|----------|------|
| 2026-05-24 | 把历史评审结论整理进标准评审日志 | 迁移到标准 project-log 结构 |

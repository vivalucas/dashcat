# 规划决策记录

## 2026-09-12：2.5.0 发布计划

- 用户明确授权推进版本、提交 GitHub main 并创建新版本标签。
- 本轮包含分页、预览和可选全局快捷键，采用次版本 2.5.0；工程构建号同步。
- 将已验证的修复、测试和已跟踪文档提交到 main，创建 annotated tag v2.5.0，推送时不强制覆盖远程引用。
- 标签触发既有 Build and Release 工作流；真实系统交互待验收项继续保留。
- Git HTTPS 连接超时，使用 GitHub Git Data API 同步相同 tree/commit/tag 对象；更新 main 时禁止强制覆盖，并核对本地与远程哈希。


## 2026-09-12：隐私、历史完整性和交互修复

- 状态：用户已明确同意；代码与文档已落实，隔离回归通过，Debug/Release构建通过，系统交互待验收项见 05/11。
- 数据方案：忽略敏感/临时剪贴板类型；文本完整保存；图片原始 PNG/TIFF 数据独立保存，缩略图另存；兼容既有 JPEG 和绝对路径记录，无法恢复历史已经丢失的尾部或分辨率。
- 存储方案：数据库查询必须完整成功才清理孤儿；容量删除保持固定项保护；增加常驻过期检查；列表后台分页和 Unicode 包含搜索，保持普通包含匹配。
- 交互方案：校验保留天数、复制前读取数据、统一默认语言、键盘导航与详情预览；增加可配置打开面板快捷键和操作反馈；Finder 失败允许手动选目录；电量提示显示防休眠状态。
- 系统方案：补齐 Apple Events entitlement；充电字段缺失显示未知；主线程不执行列表查询，禁用图片保存时不读取图片数据。
- 验证：隔离临时数据库/文件的回归检查及无签名 Debug/Release 构建；不得运行应用污染真实剪贴板历史。权限、输入法和硬件状态如无法实测，在交接中保留限制。
- 完成后：更新 01/02/03/05/06/08/09/12 及 README 的事实描述，不改变版本号或发布。


这里记录已经做过的重要规划和取舍，便于后续继续沿用。

## ADR-001：纯 AppKit，不引入 SwiftUI

- **状态**：已采用
- **背景**：菜单栏和浮窗功能简单，但要求低占用
- **方案**：用 AppKit 直接实现状态项、菜单和面板
- **备选方案**：SwiftUI + AppKit
- **依据**：更轻、更直接，符合 DashCat 的定位

## ADR-002：剪贴板使用 SQLite

- **状态**：已采用
- **背景**：需要保存历史、做过期清理、按需搜索
- **方案**：SQLite + 图片文件夹
- **备选方案**：JSON / Core Data / SwiftData
- **依据**：清理和查询都更省资源

## ADR-003：搜索采用包含匹配

- **状态**：已采用
- **背景**：用户希望输入关键词就能找到相关条目
- **方案**：大小写不敏感的普通包含匹配
- **备选方案**：全等匹配、模糊搜索、正则
- **依据**：简单、好用、实现成本低

## ADR-004：电量状态项独立显示

- **状态**：已采用
- **背景**：刘海屏和窄菜单栏空间紧张
- **方案**：独立 `NSStatusItem`，只显示极窄数字
- **备选方案**：复用猫咪状态项或显示完整百分号
- **依据**：不挤占主状态项空间

## ADR-005：Finder 新建文件只走 DashCat 菜单

- **状态**：已采用
- **背景**：需要低成本创建当前文件夹内的新文件
- **方案**：AppleScript 读取 Finder 当前目录，DashCat 菜单触发
- **备选方案**：Automator Quick Action、Finder Sync Extension
- **依据**：前者不满足空白处右键场景，后者维护成本高

## ADR-006：不做自建 API

- **状态**：已采用
- **背景**：项目是本地菜单栏工具
- **方案**：只使用系统框架和网页入口
- **备选方案**：HTTP 服务 / 本地接口层
- **依据**：没有必要增加额外边界

## 待确认项

- 无

## 2026-06-08：综合评审修复与用户体验优化计划

- **状态**：已确认，进入实现
- **背景**：综合评审发现防休眠失败回退、剪贴板图片清理一致性、文档版本口径等问题，同时确认若干剪贴板和 Finder 流程优化。
- **方案**：
  1. 修复防休眠 assertion 创建失败后 UI / 持久化状态仍显示开启的问题，失败时回退关闭。
  2. 永久保留剪贴板历史时仍执行孤儿图片清理，避免长期残留文件。
  3. 调整图片条目删除顺序，先确认数据库记录删除成功，再删除图片文件和缩略图。
  4. 清除历史增加确认弹窗，提供“清除未固定项”和“清除全部”两个明确选项。
  5. 剪贴板面板增加空状态提示，区分无历史和搜索无结果。
  6. 图片缩略图改为异步加载，减少打开面板和滚动时的主线程磁盘读取。
  7. Finder 新建文件弹窗允许输入文件名，保留重名自动递增。
  8. 更新检查期间禁用菜单项并显示进行中状态，避免重复请求。
  9. README 按当前软件实际颜色和流程同步。
- **依据**：这些改动都保持纯 AppKit、菜单即配置和低依赖路线，不引入新架构。

## 变更记录

| 日期 | 变更内容 | 原因 |
|------|----------|------|
| 2026-05-24 | 把已有长期决策整理进标准规划日志 | 迁移到标准 project-log 结构 |
